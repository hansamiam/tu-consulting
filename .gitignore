# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Stray nested checkout — sibling project tree, ignored to keep `git add -A` clean
/tu-consulting/

# Local Claude Code per-project state
.claude/

# Operational docs — kept locally on the founder's machine but not
# committed (CRM setup, outreach prospects + templates, internal
# data-correction tracking). Adding the folder here so a stray
# `git add -A` doesn't leak prospect emails / contact lists into
# the public repo. Selectively commit individual docs with
# `git add -f docs/<file>.md` if any specific doc should be tracked.
docs/

# Supabase CLI scratch
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
